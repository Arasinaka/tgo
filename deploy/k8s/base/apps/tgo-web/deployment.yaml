apiVersion: apps/v1
kind: Deployment
metadata:
  name: tgo-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tgo-web
  template:
    metadata:
      labels:
        app: tgo-web
    spec:
      containers:
        - name: tgo-web
          image: ghcr.io/tgoai/tgo/tgo-web:REPLACE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          envFrom:
            - configMapRef:
                name: tgo-web-config
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
