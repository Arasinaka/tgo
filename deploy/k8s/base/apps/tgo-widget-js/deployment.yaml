apiVersion: apps/v1
kind: Deployment
metadata:
  name: tgo-widget-js
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tgo-widget-js
  template:
    metadata:
      labels:
        app: tgo-widget-js
    spec:
      containers:
        - name: tgo-widget-js
          image: ghcr.io/tgoai/tgo/tgo-widget-js:REPLACE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          envFrom:
            - configMapRef:
                name: tgo-widget-js-config
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
