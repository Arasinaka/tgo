apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tgo-web
  namespace: dr-coffee-test
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: tgo-admin.dr-coffee.cn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tgo-web
                port:
                  name: http
  tls:
    - hosts:
        - tgo-admin.dr-coffee.cn
      secretName: tgo-admin-tls
