apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tgo-plugin-runtime-plugins
  namespace: dr-coffee-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: alicloud-disk-essd
  resources:
    requests:
      storage: 20Gi
